# Data-Trace
Data Trace is a web-based data-art project that turns user interaction into a generative particle portrait. Cursor movement, clicks, pauses, and scrolling are captured as behavioral data and transformed into a unique visual composition. Each result is also compared with aggregated statistics from previous users.
